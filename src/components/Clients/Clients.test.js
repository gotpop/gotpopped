/**
 * @jest-environment jsdom
 */

// Test info
// import info from './info';

describe('Clients', () => {

  it('should init', () => {
    // const thisButton = document.getElementById('selectInfoYears');
    // const thisBlock = document.querySelector('.select__block');
    // const goInfo = new info();

    // goInfo.init();
    // thisButton.focus();
    // const checkClassOnBlock = thisBlock.classList.contains('select__block--active');

    // expect(checkClassOnBlock).toBe(true);
  });

});
