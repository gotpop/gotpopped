/////////////////////////////////////////////////
// Remove HTML
/////////////////////////////////////////////////

while (createYearSelect.firstChild) {
    createYearSelect.removeChild(createYearSelect.firstChild);
}
